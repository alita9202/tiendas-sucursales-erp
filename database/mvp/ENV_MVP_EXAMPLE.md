# Archivo de variables de entorno (Ejemplo)

```env
# Ejemplo para conectar el Backend a la base de datos MVP
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=supermarket_mvp_db

# Cadena de conexiA³n completa (ej. Node/Prisma o TypeORM)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/supermarket_mvp_db?schema=public"
```



